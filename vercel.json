{
  "installCommand": "cd frontend && npm ci --no-audit --no-fund --loglevel=error",
  "buildCommand": "cd frontend && npm run build",
  "outputDirectory": "frontend/dist",
  "rewrites": [
    {
      "source": "/:path*",
      "destination": "/index.html"
    }
  ]
}
